# 삭제 가능 파일 목록 (정리용)

> 전부 **빌드/실행하면 다시 생기는 산출물**. 소스(.c/.h/.tcl/.py/.xdc/.md/.xpr/.xsa/Makefile)는 건드리지 않음.
> ⚠️ 단, 빌드 산출물을 지우면 **다음 board program 전에 재빌드(10~15분)**가 필요. 자주 안 굽으면 지워도 무방.

## 회수량 요약
| 묶음 | 회수 | 비고 |
|---|---|---|
| 02_zybo_skeleton Vivado/Vitis 산출물 | **~279M** | XSA+소스로 재생성 |
| 01_piper_sdk colcon build/install/log | ~11M | `colcon build` 재생성 |
| __pycache__ / tar.gz / 크래시로그 | 소량 | 자동/불필요 |
| **소계(안전)** | **~290M** | |
| (선택) piper_ros/.git | 204M | 소스는 남고 커밋이력만 사라짐 |
| **합계(공격적)** | **~494M** | |

---

## 안전 청소 — 검토 후 실행

```bash
cd /home/haeun/workspace/01_demo_260622/01_piper_fpga_testbed

# ── 02_zybo_skeleton: Vivado 산출물 (~155M) ──
find 02_zybo_skeleton -maxdepth 3 -type d \
  \( -name '*.runs' -o -name '*.gen' -o -name '*.cache' -o -name '*.hw' \
     -o -name '*.ip_user_files' -o -name '.Xil' -o -name '.rigel_lopper' \) -prune -print
#   ↑ 먼저 -print로 목록 확인 후, 맞으면 끝에 -exec rm -rf {} + 붙여 실행

# ── 02_zybo_skeleton: Vitis 산출물 (~124M) ──
#   각 단계의  *_vitis/**/export,  vitis_ws/**/{build,_ide,export},  _ide  폴더
#   예: 05_zybo_can_eth/vitis_ws/udp_can_app/{build,_ide}, .../zybo_eth/export
#       05_zybo_can_eth/zybo_can_eth_vitis  (GUI 워크스페이스 통째 — 활성은 vitis_ws)

# ── 01_piper_sdk: colcon 산출물 (~11M) ──
rm -rf 01_piper_sdk/piper-hil-docker/ros2_ws/{build,install,log}

# ── 파이썬 캐시 / 임시 (전 폴더) ──
find . -type d -name '__pycache__' -prune -exec rm -rf {} +
rm -f 02_zybo_skeleton/05_zybo_can_eth/hs_err_pid*.log
rm -f 03_hils/piper_pc_handoff.tar.gz          # 이미 풀려있음

# ── (선택) git 히스토리 204M ── 소스는 남음. 이력 필요없을 때만.
# rm -rf 01_piper_sdk/piper-hil-docker/ros2_ws/src/piper_ros/.git
```

## 폴더별 상세 (각 폴더의 `<폴더이름>.md` 의 "삭제 가능" 절 참고)
| 폴더 | 산출물 회수 |
|---|---|
| 02_zybo_skeleton/01_zybo_test | ~34M |
| 02_zybo_skeleton/02_zybo_can | ~74M |
| 02_zybo_skeleton/04_zybo_can_v2 | ~40M |
| 02_zybo_skeleton/05_zybo_can_eth | ~131M |
| 01_piper_sdk/piper-hil-docker | ~11M (+204M git 선택) |
| 03_arduino_due_cantest, bin | 0 (전부 유지) |

## ⚠️ 절대 지우지 말 것
- `*.xsa` (하드웨어 정의 — 이게 있어야 재빌드 가능. 단 중복 사본은 1개만 남기면 됨)
- `*.xpr`, `src/*.c`, `*.xdc`, `*.tcl`, `build_*.py`, `Makefile`, 모든 `.md`
- `bin/doc/*` (계약서), `03_arduino_due_cantest/*` (스케치)
- `design_1.bd` 가 든 `zybo_can_v2_vivado/...srcs/` (블록디자인 원본 — `.gen`은 지워도 됨)
